this folder is for server actions like user login actions, rubric actions, user actions etc ...
the actions are stored in a shared file example (rubricActions) contains rubric actions and rubric related actions only .. 